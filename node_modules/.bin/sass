#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/D:/Work/MY/XLGlobeWebV1.0/node_modules/.store/sass@1.89.2/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/D:/Work/MY/XLGlobeWebV1.0/node_modules/.store/sass@1.89.2/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../.store/sass@1.89.2/node_modules/sass/sass.js" "$@"
else
  exec node  "$basedir/../.store/sass@1.89.2/node_modules/sass/sass.js" "$@"
fi
